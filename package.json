{
  "name": "kaninfarmen",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --port 8731",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --port 8732",
    "test": "vitest run"
  },
  "dependencies": {
    "phaser": "^3.87.0"
  },
  "devDependencies": {
    "puppeteer-core": "^25.10.0",
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "vitest": "^3.0.0"
  }
}
